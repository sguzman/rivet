{
  "$schema": "https://schema.tauri.app/config/2",
  "productName": "Rivet",
  "version": "0.1.0",
  "identifier": "io.rivet.desktop",
  "build": {
    "beforeDevCommand": "bash -lc 'ROOT=\"$(git rev-parse --show-toplevel 2>/dev/null || pwd)\"; exec trunk serve --config \"$ROOT/crates/rivet-gui/ui/Trunk.toml\" --port 1420'",
    "beforeBuildCommand": "bash -lc 'ROOT=\"$(git rev-parse --show-toplevel 2>/dev/null || pwd)\"; exec trunk build --config \"$ROOT/crates/rivet-gui/ui/Trunk.toml\"'",
    "devUrl": "http://localhost:1420",
    "frontendDist": "../ui/dist"
  },
  "app": {
    "windows": [
      {
        "title": "Rivet",
        "width": 1280,
        "height": 820,
        "resizable": true
      }
    ],
    "security": {
      "csp": null
    }
  }
}
