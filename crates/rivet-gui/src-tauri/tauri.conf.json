{
  "$schema": "https://schema.tauri.app/config/2",
  "productName": "Rivet",
  "version": "0.1.0",
  "identifier": "io.rivet.desktop",
  "build": {
    "beforeDevCommand": "bash -lc 'ROOT=\"$(git rev-parse --show-toplevel 2>/dev/null || pwd)\"; exec pnpm --dir \"$ROOT/crates/rivet-gui/ui\" run dev --host --port 1420'",
    "beforeBuildCommand": "bash -lc 'ROOT=\"$(git rev-parse --show-toplevel 2>/dev/null || pwd)\"; exec pnpm --dir \"$ROOT/crates/rivet-gui/ui\" run build'",
    "devUrl": "http://localhost:1420",
    "frontendDist": "../ui/dist"
  },
  "app": {
    "enableGTKAppId": true,
    "windows": [
      {
        "title": "Rivet",
        "width": 1280,
        "height": 820,
        "resizable": true,
        "decorations": false
      }
    ],
    "security": {
      "csp": null
    }
  },
  "bundle": {
    "icon": [
      "icons/mascot-square.png",
      "icons/favicon-32x32.png",
      "icons/icon.png",
      "icons/icon.ico",
      "icons/icon.icns"
    ]
  }
}
